#!/usr/bin/env bash

# Process a given Brewfile with homebrew commands.

# Usage: brew-bundle -f Brewfile
# Author: Marcus André - https://github.com/marcusandre
# License: MIT

set -e

#
# Print usage information
#

function usage() {
  echo "Usage: brew-bundle -f Brewfile" &&  exit 1
}

#
# Process given file
#

function process() {
  local Brewfile=$1

  while read recipe; do
    brew $recipe
  done <$Brewfile
}

#
# Parse <args>
#

if [[ $# -ne 2 || "$1" != "-f" ]]; then
  usage && exit 1
else
  process $2 && exit 0
fi
