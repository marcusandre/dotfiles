#!/usr/bin/env bash

delim_start="{{"
delim_end="}}"

git_hash=$(git rev-parse --short HEAD | cut -c -7 | xargs)
git_branch=$(git symbolic-ref --short -q HEAD)

echo $delim_start$git_hash@$git_branch$delim_end
